package oops;

public class InterfaceExample {
	/*
	 * Definition
	 * 
	 * -->Any Service requirement specification ( SRS )
	 *
	 * -->Any contract between client and service provider is considered as
	 * interface
	 * 
	 * --> Interface considered as pure abstract class (100% ) but from 1.9 onwards
	 * it allows default methods and private methods
	 * 
	 * 
	 * Every method present in interface is always abstract and public
	 * 
	 * while overriding we can't reduce the scope of method
	 * 
	 * Class implementing the interface, then it should give implementation for all
	 * the methods in interface
	 */
}
